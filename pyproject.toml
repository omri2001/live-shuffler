[project]
name = "live-shuffler"
version = "0.1.0"
description = "Spotify-powered intelligent shuffle based on audio metrics"
requires-python = ">=3.11"
dependencies = [
    "mkdocs>=1.6",
    "mkdocs-material>=9.5",
]
